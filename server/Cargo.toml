[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
#bincode = { version = "1.3.2" }
serde = { version = "1.0.219", features = ["derive"] }
shared = { path = "../shared"}
bevy_simplenet = { version = "0.15.2" }
enfync = { version = "0.1.6" }
